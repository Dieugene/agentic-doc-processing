# Технический план: CLI Interface для Document Processing

## 1. Анализ задачи

Необходимо создать интерфейс командной строки для обработки документов через полный pipeline. Пользователь сможет запустить обработку документа (DOCX/XLSX/PDF) и получить DocumentSkeleton с зафиксированной структурой, сохраненный в FileStorage.

Pipeline уже реализован в модулях (Converter → Renderer → VLM-OCR → SkeletonBuilder → FileStorage), но "оркестратор" DocumentProcessor существует только в тестах (`conftest.py`). CLI сделает pipeline доступным для использования.

## 2. Текущее состояние

### Существующие компоненты (используем без изменений):

| Модуль | Файл | Назначение |
|--------|------|------------|
| Converter | `processing/converter.py` | DOCX/Excel/TXT → PDF |
| Renderer | `processing/renderer.py` | PDF → PNG images |
| VLMOCRExtractor | `processing/vlm_ocr_extractor.py` | Обёртка над VLM-OCR |
| SkeletonBuilder | `processing/skeleton_builder.py` | DocumentData → DocumentSkeleton |
| FileStorage | `storage/file_storage.py` | JSON persistence |
| DocumentSkeleton | `document/skeleton.py` | Структура документа |
| MockVLMOCR | `processing/mock_vlm_ocr.py` | Mock для тестирования |

### Тестовый оркестратор:

**Файл:** `processing/tests/integration/conftest.py:72-147`

Класс `DocumentProcessor` - тестовый оркестратор pipeline. Помечен как "NOT part of production code". Содержит готовую логику:
1. Detect file type
2. Convert to PDF (если нужно)
3. Render to PNG
4. Extract via VLM-OCR
5. Build DocumentSkeleton
6. Save to FileStorage

**Нужно:** Перенести/адаптировать в продакшен-код с улучшениями (логирование прогресса).

## 3. Предлагаемое решение

### 3.1. Общий подход

1. **Создать DocumentProcessor** в `processing/processor.py` - продакшен версия оркестратора
2. **Создать CLI module** в `processing/cli.py` - точка входа `python -m processing.cli`
3. **Обновить `__init__.py`** для экспорта компонентов

CLI использует MockVLMOCR (заглушку) - реальный VLM-OCR будет в задаче 014.

### 3.2. Компоненты

#### Компонент A: DocumentProcessor

**Файл:** `02_src/processing/processor.py`

**Назначение:** Оркестратор полного pipeline для продакшен использования.

**Интерфейс:**
```python
class DocumentProcessor:
    def __init__(self, vlm_ocr_module, storage_base_path: str, renderer_dpi: int)
    async def process_document(self, file_path: str) -> str  # → document_id
```

**Логика работы:**
1. Detect file type (через `Converter.detect_file_type`)
2. Convert to PDF если нужно (через `Converter.convert_to_pdf`)
3. Render PDF to PNG images (через `Renderer.render_pdf_to_images`)
4. Extract data via VLM-OCR (через `VLMOCRExtractor.extract_full_document`)
5. Build DocumentSkeleton (через `SkeletonBuilder.build_skeleton`)
6. Save to FileStorage (через `FileStorage.save_skeleton`)

**Отличия от тестовой версии:**
- Добавляет callback для прогресса (каждый этап pipeline)
- Генерирует document_id как `doc_{uuid_hex[:16]}`
- Передаёт все параметры через `__init__` (внедрение зависимостей)

**Зависимости:**
- `Converter` из `processing.converter`
- `Renderer` из `processing.renderer`
- `VLMOCRExtractor` из `processing.vlm_ocr_extractor`
- `SkeletonBuilder` из `processing.skeleton_builder`
- `FileStorage` из `storage.file_storage`
- `uuid` (стандартная библиотека)

#### Компонент B: CLI Module

**Файл:** `02_src/processing/cli.py`

**Назначение:** Интерфейс командной строки для обработки документов.

**Интерфейс:**
```bash
python -m processing.cli <file_path> [--output-dir DIR] [--dpi DPI]
```

**Аргументы:**
- `file_path` (positional): Путь к документу
- `--output-dir` (optional): Директория для skeleton'ов (default: `04_storage/skeletons`)
- `--dpi` (optional): DPI для рендеринга (default: 200)

**Логика работы:**
1. Парсит аргументы через `argparse`
2. Проверяет существование файла
3. Создаёт `DocumentProcessor` с `MockVLMOCR`
4. Вызывает `process_document()` с callback для прогресса
5. Выводит прогресс каждого этапа (step-by-step)
6. Загружает сохранённый skeleton
7. Выводит структуру документа в виде дерева

**Формат вывода прогресса:**
```
📄 Загрузка файла: my_document.docx
✅ Конвертация: DOCX → PDF (2.3s)
✅ Рендеринг: PDF → 5 изображений (1.1s)
✅ VLM-OCR: извлечение данных (8.4s)
✅ Skeleton Builder: агрегация (0.3s)
✅ FileStorage: сохранение (0.1s)
💾 Сохранено: 04_storage/skeletons/doc_abc123.json

🌳 Структура документа:
root: [root] doc_abc123 (стр. 1-5)
  └── section_1: [section] 1. Раздел (стр. 1-2)
      └── table_1: [table] Таблица 1.1 (стр. 2)
```

**Обработка ошибок:**

| Ситуация | Действие |
|----------|----------|
| Файл не существует | `❌ Файл не найден: {path}` + exit 1 |
| Неподдерживаемый формат | `❌ Неподдерживаемый формат: .txt` + список поддерживаемых |
| Ошибка конвертации | `❌ Ошибка конвертации: {error}` + traceback |
| Ошибка VLM-OCR | `❌ Ошибка VLM-OCR: {error}` + traceback |
| Ошибка сохранения | `❌ Ошибка сохранения: {error}` + traceback |

**Функция `print_tree`:**
- Рекурсивный вывод узла и детей
- Формат: `indent + connector + node_id: [type] title (стр. start-end)`
- Использует `NodeType` и `PageRange` из skeleton

**Зависимости:**
- `argparse` (стандартная библиотека)
- `asyncio` (стандартная библиотека)
- `sys` (стандартная библиотека)
- `pathlib` (стандартная библиотека)
- `DocumentProcessor` из `processing.processor`
- `MockVLMOCR` из `processing.mock_vlm_ocr`
- `DocumentSkeleton`, `NodeType` из `document`

#### Компонент C: Обновление __init__.py

**Файл:** `02_src/processing/__init__.py`

**Добавить экспорты:**
```python
from .processor import DocumentProcessor
```

**Назначение:** Удобство импорта для CLI и будущих пользователей.

### 3.3. Структуры данных

Используются существующие структуры из `document/skeleton.py`:
- `DocumentSkeleton` - скелет документа
- `Node` - узел скелета
- `NodeType` - enum типов узлов (ROOT, CHAPTER, SECTION, TABLE, etc.)
- `PageRange` - диапазон страниц

Новые структуры не требуются.

### 3.4. Ключевые алгоритмы

#### Алгоритм DocumentProcessor.process_document:

1. Detect file type → `FileType` enum
2. Если не PDF: convert_to_pdf → временный PDF файл
3. render_pdf_to_images → `List[bytes]` (PNG images)
4. vlm_extractor.extract_full_document(images) → `DocumentData`
5. Генерирует `document_id = f"doc_{uuid.uuid4().hex[:16]}"`
6. skeleton_builder.build_skeleton(document_data, document_id) → `DocumentSkeleton`
7. storage.save_skeleton(document_id, skeleton)
8. Возвращает `document_id`

**Прогресс:** Callback вызывается после каждого этапа с сообщением и временем.

#### Алгоритм CLI.main:

1. Парсит аргументы → `args`
2. Проверяет `Path(args.file_path).exists()`
3. Создаёт `DocumentProcessor(vlm_ocr_module=MockVLMOCR(), ...)`
4. Определяет callback для прогресса (вывод с эмодзи)
5. Вызывает `await processor.process_document(file_path)` → `document_id`
6. Загружает skeleton через `storage.load_skeleton(document_id)`
7. Получает root через `skeleton.get_root()`
8. Вызывает `print_tree(root, indent=0)` для вывода структуры

### 3.5. Изменения в существующем коде

**Минимальные изменения:**

1. **`02_src/processing/__init__.py`** - добавить экспорт `DocumentProcessor`
2. **Остальное** - только создание новых файлов

**НЕ меняется:**
- Конвертер, Renderer, VLM-OCR Extractor, SkeletonBuilder, FileStorage
- DocumentSkeleton, Node, NodeType
- MockVLMOCR

## 4. План реализации

1. **Создать `02_src/processing/processor.py`**
   - Класс `DocumentProcessor`
   - Метод `process_document` с callback для прогресса
   - Импорты всех зависимостей

2. **Создать `02_src/processing/cli.py`**
   - Функция `print_tree` для вывода дерева
   - Асинхронная функция `main`
   - `argparse` для аргументов
   - Обработка ошибок с понятными сообщениями
   - `if __name__ == "__main__"` блок

3. **Обновить `02_src/processing/__init__.py`**
   - Добавить экспорт `DocumentProcessor`

4. **Тестирование CLI**
   - Запуск с тестовым DOCX файлом
   - Проверка вывода прогресса
   - Проверка сохранения skeleton
   - Проверка вывода дерева

## 5. Технические критерии приемки

- [ ] TC-001: CLI запускается как `python -m processing.cli document.docx`
- [ ] TC-002: Поддерживаются форматы: DOCX, XLSX, PDF
- [ ] TC-003: CLI выводит прогресс каждого этапа с временем выполнения
- [ ] TC-004: Skeleton сохраняется в `04_storage/skeletons/<document_id>.json/skeleton.json`
- [ ] TC-005: После обработки CLI выводит структуру документа в виде дерева
- [ ] TC-006: Обработка ошибок: файл не существует → exit code 1 с сообщением
- [ ] TC-007: Обработка ошибок: неподдерживаемый формат → список поддерживаемых
- [ ] TC-008: DocumentProcessor использует MockVLMOCR (заглушка)
- [ ] TC-009: DocumentProcessor генерирует уникальный document_id
- [ ] TC-010: CLI поддерживает `--output-dir` и `--dpi` аргументы

## 6. Важные детали для Developer

### Особенности MockVLMOCR

**MockVLMOCR** - заглушка для тестирования. Возвращает фейковые данные:
- Текст: "Mock document text from N pages"
- Структура: 1-2 секции с заголовками
- Таблицы: 2 таблицы (NUMERIC и TEXT_MATRIX)

**Важно:** После CLI задачей 014 будет интеграция реального VLM-OCR.

### Детали реализации DocumentProcessor

**Callback для прогресса:**
- Сигнатура: `callback(step_name: str, duration_sec: float, details: str = "")`
- Вызывается после каждого этапа pipeline
- Используется в CLI для вывода прогресса

**Временные файлы:**
- `Converter.convert_to_pdf` создаёт временный PDF
- Временный файл НЕ удаляется (может быть полезен для отладки)
- Будущие улучшения: cleanup опция

**Асинхронность:**
- Все методы pipeline async (`await`)
- CLI.main - async функция
- Запуск: `asyncio.run(main())`

### Детали CLI

**Unicode в Windows:**
- Используй стандартные шрифты (Arial, Times) в Converter
- Эмодзи в CLI работают в Windows Terminal

**Аргумент `--dpi`:**
- Минимум: 150 (предупреждение в Renderer)
- Рекомендуется: 200-300 для VLM-OCR
- Максимум: не ограничен (предупреждение >300)

**Структура вывода дерева:**
- Используй отступы `"  " * indent`
- Коннектор: `"└── "` для потомков, пусто для root
- Формат: `prefix + connector + node.id: [type] title (page_range)`

### Обработка ошибок

**Важно:** Разные типы исключений требуют разного вывода:

```python
# FileNotFoundError
print(f"❌ Файл не найден: {file_path}")

# ValueError (неподдерживаемый формат)
print(f"❌ Неподдерживаемый формат: {ext}")
print(f"Поддерживаемые форматы: {', '.join([...])}")

# Остальные ошибки
print(f"❌ Ошибка: {error}")
import traceback
traceback.print_exc()
```

**Exit codes:**
- Успех: implicit exit 0
- Ошибка: `sys.exit(1)`

### Временные файлы

**Converter** создаёт временный PDF через `tempfile.NamedTemporaryFile(delete=False)`. Файл остаётся в файловой системе после обработки.

**Примечание:** Для v1.0 это приемлемо. Будущие улучшения: cleanup опция в DocumentProcessor.

### Зависимости от Poppler

**Renderer** требует Poppler (pdf2image). Если Poppler не установлен:
- `Renderer.__init__` выбросит `ImportError`
- CLI должен перехватить и вывести понятное сообщение:
  ```
  ❌ Poppler не установлен. Установите с https://github.com/oschwartz10612/poppler-windows/releases/
  ```

### Логирование

**Не добавляй logging в CLI** - используй print для вывода пользователю.
**DocumentProcessor** может использовать logging для внутренних ошибок.

### Тестирование

**Для тестирования CLI:**
1. Создай тестовый DOCX файл (можно через python-docx)
2. Запусти: `python -m processing.cli test.docx`
3. Проверь:
   - Вывод прогресса
   - Создан файл `04_storage/skeletons/doc_<id>.json/skeleton.json`
   - Вывод структуры документа

**Минимальный тест:**
```bash
cd 02_src
python -m processing.cli ../path/to/document.docx
```
